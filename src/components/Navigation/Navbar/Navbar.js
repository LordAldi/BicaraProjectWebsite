import NavigationItems from "../NavigationItems/NavigationItems";

export default function Navbar() {
  return (
    <header>
      <nav>
        <NavigationItems />
      </nav>
    </header>
  );
}
